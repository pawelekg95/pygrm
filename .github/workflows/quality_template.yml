name: Code quality template
on:
  workflow_call:
    inputs:
      module_name:
        required: true
        type: string

jobs:
  Pylint:
    runs-on: self-hosted
    steps:
      - run: cd ${{ github.workspace }}
      - run: pylint ${{ inputs.module_name }}
